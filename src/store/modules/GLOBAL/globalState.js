import actions      from './actions'
import mutations    from './mutations'
import * as types   from './types'

const state = {
    deviceInfo: {}      // 设备信息
}

export default {
    state,
    types,
    actions,
    mutations
}