import actions      from './actions'
import mutations    from './mutations'
import * as types   from './types'

const state = {
    bulletinInfo: {}
}

export default {
    state,
    types,
    actions,
    mutations
}